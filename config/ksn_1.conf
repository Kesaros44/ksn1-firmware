# NOTE: This file is NOT picked up by the ZMK build system for split
# keyboards. Only files named exactly after the shield target
# (ksn_1_left.conf / ksn_1_right.conf, in
# config/boards/shields/ksn_1/) are used.
#
# The settings that used to live here (including
# CONFIG_NFCT_PINS_AS_GPIOS=y, needed to use P0.09/P0.10 as GPIO
# instead of NFC antenna pins) have been moved to:
#   config/boards/shields/ksn_1/ksn_1_left.conf
#   config/boards/shields/ksn_1/ksn_1_right.conf
#
# Keeping this file around for reference only; it has no effect on
# the build and can be safely deleted.
